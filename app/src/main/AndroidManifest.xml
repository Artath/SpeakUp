<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.artem.speakup">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".Authentication.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Authentication.AuthenticationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RecordActivity" />
        <activity android:name=".TonguesTwisters.TongueTwistersActivity" />
        <activity android:name=".TimeSpeechAssistentBadRealize.AssistantActivity" />
        <activity android:name=".TimeSpeechAssistentBadRealize.CreatePartActivity" />
        <activity android:name=".TimeSpeechAssistentBadRealize.CreateSessionActivity" />
        <activity android:name=".TimeSpeechAssistentBadRealize.SessionsActivity" />
        <activity android:name=".TimeSpeechAssistentBadRealize.PartsActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SpeechAnalysis.SpeechRecordsActivity" />
        <activity android:name=".SpeechAnalysis.AnalysisResActivity" />
        <activity android:name=".TrueMainActivity" />
        <activity android:name=".Authentication.TrulyTrueMainActivity"></activity>
    </application>

</manifest>